<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <Compile Include="..\SimpleSyncServer.cs" />
  </ItemGroup>

  <ItemGroup>
    <!-- Исключаем файлы, которые не нужны для сервера синхронизации -->
    <Compile Remove="..\Program.cs" />
    <Compile Remove="..\PokerServer.cs" />
    <Compile Remove="..\ClientConnection.cs" />
    <Compile Remove="..\GameSession.cs" />
    <Compile Remove="..\CloudSyncService.cs" />
    <Compile Remove="..\UserDatabase.cs" />
  </ItemGroup>

</Project>

